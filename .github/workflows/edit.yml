name: "edit"

# yamllint disable-line rule:truthy 
on:
  push:
    branches: [xr]
permissions:
  contents: write
jobs:
  edit:
    name: edit
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: notes
          fetch-depth: 0

      - name: inspect builder
        run: |
          git config --global user.email "jackhunt43@libero.it"
          git config --global user.name "dragonhunt02"

          #git fetch origin
          #git checkout main
          #git checkout roadmap
          #git branch -f roadmap main
          #git push -f origin roadmap
          #git reset --hard origin/main
          
          # rm .github/workflows/update-roadmap.yml
          #git reset --hard HEAD~1

          git reset --soft HEAD~7
          git add -A
          git commit -a -m "Add notes column to roadmap"
          git push -f
